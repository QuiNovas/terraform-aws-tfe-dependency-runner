{
  "swagger": "2.0",
  "info": {
    "version": "0.1.0",
    "title": "tfe-dependency-runner"
  },
  "basePath": "/webhook",
  "schemes": [
    "https"
  ],
  "paths": {
    "tfe-dependency-runner": {
      "post": {
        "produces": [
          "application/json"
        ],
        "responses": {
          "200": {
            "description": "200 response",
            "schema": {
              "$ref": "#/definitions/Empty"
            }
          }
        },
        "x-amazon-apigateway-integration": {
          "uri": "${tfe_dependency_runner_uri}",
          "responses": {
            "default": {
              "statusCode": "200"
            }
          },
          "passthroughBehavior": "when_no_match",
          "httpMethod": "POST",
          "contentHandling": "CONVERT_TO_TEXT",
          "type": "aws_proxy"
        }
      }
    }
  },
  "definitions": {
    "Empty": {
      "type": "object",
      "title": "Empty Schema"
    }
  }
}